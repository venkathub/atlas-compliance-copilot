"""Atlas evaluation & observability harness (RAGAS/DeepEval).

P0: scaffold only — exists so CI exercises the Python toolchain (ruff + pytest).
The real eval harness is built in P2.
"""

__version__ = "0.1.0"

# mcp-tools (Spring Boot) — **stub**

Governed enterprise actions exposed over MCP (Streamable HTTP, OAuth 2.1 resource
server). Hosts the draft-SAR write tool with human-in-the-loop approval, per-call
clearance re-check, and an append-only audit log.

**Built in P4.** Empty placeholder for now (monorepo layout, P0).

# ui (React) — **stub**

Chat + admin UI: streamed answers, inline citations, agent execution trace, eval/cost
dashboards, audit-log views. Output is sanitized before render (no XSS via citations).

**Built in P5.** Node version pinned via `.nvmrc` (repo root). Empty placeholder for
now (monorepo layout, P0).

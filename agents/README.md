# agents (Python / LangGraph) — **stub**

Planner–executor agent orchestration: durable Postgres checkpointer, human-in-the-loop
via `interrupt` → `Command(resume)` / MCP elicitation, and MCP tool calls into
`mcp-tools`.

**Built in P4.** Empty placeholder for now (monorepo layout, P0).

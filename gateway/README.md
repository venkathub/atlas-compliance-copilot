# gateway (Spring Boot) — **stub**

Cost-aware API gateway: simulated-IdP auth, model routing (cost/latency/quality),
semantic caching, rate limiting, PII egress redaction, budget spend-caps + circuit
breaker, output sanitization, and token/cost/latency dashboards (Micrometer).

**Built in P3.** Empty placeholder for now (monorepo layout, P0).
